__pycache__/
*.pyc
.venv/
venv/
env/

# Local data / logs / models
logs/
# data/*.db
data/*.sqlite
*.db-wal
*.db-shm
*.pt
*.pkl
*.joblib

# Scraped artifacts
*.zip
*.csv
*.json.gz

# Exception: ground-truth label CSVs are checked in so teammates can sync
# without re-scraping Wikipedia. Exported by data/export_labels.py.
!data/labels/
!data/labels/*.csv

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
.env
.env.*

# Secrets — cloud service-account / API key files
# (specific known leaks first, broader patterns after)
*service-account*.json
*service_account*.json
*credentials*.json
*-credentials.json
gcp-key*.json
google-key*.json
*.pem
*.p12
*.pfx
*.key
secrets/
**/secrets.*

# Claude Code local settings (machine-specific, can contain local paths)
.claude/settings.local.json
